/**
 * @jsx React.DOM
 */

'use strict';
var React = require('react');

var BusStops = React.createClass({
  render() {
    return (
      <div className="mod-bus-stops-container">
        Nearby bus stops info
      </div>
    );
  }
});

module.exports = BusStops;
